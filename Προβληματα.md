# Κύρια Προβλήματα στο Dataset και στα Δεδομένα της Εφαρμογής

## 1. Ορισμός Χρήστη (User Identification Problem)

Στο dataset υπάρχουν δύο διαφορετικά πεδία ταυτοποίησης χρήστη:

- `user_id`
- `user_pseudo_id`

Παρατηρήθηκαν τα εξής προβλήματα:

- Για πολλούς `user_id` υπάρχουν **πολλαπλά pseudo_user_id**.
- Για πολλούς `pseudo_user_id` υπάρχουν **πολλαπλά user_id**.

Αυτό σημαίνει ότι:
- Το `user_id` δεν είναι αξιόπιστος δείκτης μοναδικού χρήστη.
Μετά απο διευκρίνιση: 
- Το `pseudo_user_id` αντιστοιχεί **στo κατέβασμα της εφαρμογής στην συγκεκριμένη συσκευή** και όχι στον πραγματικό χρήστη.
(Αν ο χρήστης διαγράψει και κατεβάσει ξανά την εφαρμογή τότε παίρνει άλλο pseudo id.)

Άρα υπάρχει σοβαρό πρόβλημα στο **πώς ορίζουμε έναν χρήστη**, ειδικά για αναλύσεις όπως:
- clustering χρηστών
- behavioural analysis
- user segmentation

Yπάρχει κίνδυνος να **μετράμε τον ίδιο χρήστη πολλές φορές**.

Ταυτόχρονα παρατηρήθηκε πως το συγκεκριμένο πρόβλημα βασίζεται στο τρόπο που γίνεται το authentication του χρήστη στην εφαρμογή. 
Συγκεκριμένα στην εφαρμογή android, υπάρχει τεράστιο πρόβλημα με το sign out button, όπου μετά το sign out, εάν ο χρήστης επιλέξει να μην επανασυνδεθεί και πατήσει το κουμπί not now , η εφαρμογή τον κρατάει συνδεδεμένο. 

Συνολικό pipeline για αυτό το πρόβλημα:
Open app
Sign in
Go to Profile page
Sign out
Click not now
Go to Profile again
Ο χρήστης είναι ακόμα συνδεδεμένος. 

Το ίδιο παρατηρείται ακόμα και αν ο χρήστης συνδεθεί με άλλο account. Εάν αποσυνδεθεί και ακολουθήσει το απο πάνω pipeline , τότε πάλι θα βρεθεί συνδεδεμένος, ενώ έχει κάνει sign out. 



---

# 2. Διαφορές μεταξύ Android και iOS δεδομένων

Τα δεδομένα από τις δύο πλατφόρμες έχουν διαφορετική ποιότητα και διαφορετικά events.

### Android
Τα Android δεδομένα περιέχουν πιο πλήρη events όπως:

- `start_tour`
- `story_start`
- `story_end`
- `tour_end`

Αυτό επιτρέπει να ανακατασκευαστεί σχετικά εύκολα το user journey.

### iOS
Τα iOS δεδομένα **δεν περιέχουν σημαντικά events**, όπως:

- `story_start`
- `story_end`

Αντί αυτών υπάρχουν μόνο:

- `time_update`
- `story_listened_20`
- `story_listened_40`
- `story_listened_60`
- `story_listened_80`

Αυτό δημιουργεί σημαντικά προβλήματα:

- Δεν γνωρίζουμε ακριβώς πότε ξεκινά μια ιστορία.
- Δεν γνωρίζουμε πότε ολοκληρώνεται μια ιστορία.
- Είναι δύσκολο να υπολογιστεί **drop-off**, **completion** ή **story order behaviour**.

---

# 3. Πρόβλημα στο Story Order

Κάθε tour αποτελείται από πολλές ιστορίες (`story_id`) οι οποίες έχουν συγκεκριμένη σειρά.

Η σωστή σειρά υπάρχει στο αρχείο:

- `story_details.csv`

Ωστόσο:

- Κάποια `story_id` που υπάρχουν στο CSV **δεν εμφανίζονται καθόλου στα events**.
- Αυτό σημαίνει ότι η "θεωρητική σειρά" ιστοριών **δεν ταιριάζει πάντα με τα δεδομένα χρήσης**.

Για να χρησιμοποιηθεί το story order ως feature (π.χ. αν οι χρήστες ακολουθούν τη σωστή σειρά), πρέπει πρώτα να:

- αφαιρεθούν ιστορίες που δεν υπάρχουν στα events
- δημιουργηθεί μια **διορθωμένη σειρά ιστοριών ανά tour**. 


---



---

# 4. Outliers και πιθανές Test εγγραφές

Βρέθηκε ένας ακραίος χρήστης:

- `user_id = 0`
- περίπου **800.000 events**

Ενώ ο επόμενος μεγαλύτερος χρήστης έχει περίπου **10.000 events**.

Επιπλέον:

- ο ίδιος χρήστης έχει περίπου **8.000 διαφορετικά pseudo_user_id**

Αυτό πιθανότατα σημαίνει ότι:

- πρόκειται για **test account ή admin activity**

και πρέπει να **αφαιρεθεί από την ανάλυση** γιατί αλλοιώνει σημαντικά τα στατιστικά.


---

# 5. Trigger Events με ίδιο Timestamp

Υπάρχουν εκατοντάδες χιλιάδες events που συμβαίνουν **στο ίδιο ακριβώς δευτερόλεπτο**.

Πχ 

- `Story_started`
- `story_listened_20`
- `story_listened_40`
- `story_listened_60`
- `story_listened_80`
- `story_completed`

Αυτό υποδεικνύει ότι:

- κάποια events παράγονται αυτόματα από το app
- λειτουργούν ως **system triggers** και όχι ως πραγματικές ενέργειες χρήστη.

Αν δεν αφαιρεθούν ή φιλτραριστούν:

- μπορούν να επηρεάσουν αρνητικά την ανάλυση behaviour.
